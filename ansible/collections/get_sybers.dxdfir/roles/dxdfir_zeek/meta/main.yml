---
galaxy_info:
  role_name: dxdfir_zeek
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process PCAPs into Zeek JSON logs for the Elastic-native or SOF-ELK pipeline. Invokes the
    get_sybers_dxdfir.zeek Python processor; one folder of *.json per capture.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, zeek, pcap, network]

dependencies: []

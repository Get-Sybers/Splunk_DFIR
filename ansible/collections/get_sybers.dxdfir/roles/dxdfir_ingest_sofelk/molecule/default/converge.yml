---
- name: Converge
  hosts: localhost
  gather_facts: true
  vars:
    dxdfir_ingest_sofelk_src_dir: /tmp/dxdfir_ingest_sofelk_molecule/src
    dxdfir_ingest_sofelk_target_dir: /tmp/dxdfir_ingest_sofelk_molecule/target
    dxdfir_ingest_sofelk_python_path: "{{ (playbook_dir + '/../../../../../../../python') | realpath }}"
  tasks:
    - name: "converge | run dxdfir_ingest_sofelk"
      ansible.builtin.include_role:
        name: dxdfir_ingest_sofelk

---
galaxy_info:
  role_name: dfir_zeek
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process PCAPs into Zeek JSON logs for the ADX or SOF-ELK pipeline. Invokes the
    get_sybers_dfir.zeek Python processor; one folder of *.json per capture.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, zeek, pcap, network]

dependencies: []

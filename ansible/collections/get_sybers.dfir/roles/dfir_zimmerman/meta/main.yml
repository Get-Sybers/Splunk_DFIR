---
galaxy_info:
  role_name: dfir_zimmerman
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Process disk images with Eric Zimmerman's EZ-Tools (RECmd, JLECmd, LECmd,
    AmcacheParser, AppCompatCacheParser, SBECmd, RBCmd, MFTECmd) plus a plaso-driven
    SRUM parse into per-host artefact output for the ADX or SOF-ELK pipeline.
    Invokes the get_sybers_dfir.zimmerman Python processor; one output dir per host.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, zimmerman, eztools, registry, amcache, srum, forensics]

dependencies: []

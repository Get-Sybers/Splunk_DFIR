---
galaxy_info:
  role_name: dfir_evtx
  namespace: get_sybers
  author: Get-Sybers
  description: >-
    Parse Windows Event Logs (.evtx) with EvtxECmd into normalised JSON for the Elastic-native
    or SOF-ELK pipeline. Invokes the get_sybers_dfir.evtx Python processor; one
    <base>_EvtxECmd_Output.json per log, grouped by source host.
  company: Get-Sybers
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    - name: Debian
      versions: [bookworm, trixie]
  galaxy_tags: [dfir, evtx, windows, host]

dependencies: []

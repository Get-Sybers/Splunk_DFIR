[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "get-sybers-dfir"
version = "0.5.0"
description = "DX_DFIR forensic processing package (zeek, velociraptor, evtx, volatility, plaso, signatures) + dxdfir CLI"
requires-python = ">=3.10"
license = { text = "MIT" }
dependencies = [
    "typer>=0.12",
    # The CLI drives the get_sybers.dfir collection with ansible-playbook, so it
    # ships with the package — installing the CLI installs a working ansible-playbook.
    "ansible-core>=2.16",
    # `dxdfir deploy` / `ingest` drive community.docker, whose modules import the
    # Python docker SDK (requests + docker); without these a clean install (and
    # the offline wheel bundle) cannot bring the emulator up.
    "requests>=2.31",
    "docker>=7.0",
]

[project.scripts]
# `dxdfir` front-end (Typer). The processors also run standalone via
# `python -m get_sybers_dfir.<source>`.
dxdfir = "get_sybers_dfir.cli:app"

[tool.setuptools.packages.find]
include = ["get_sybers_dfir*"]
